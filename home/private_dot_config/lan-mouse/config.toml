{{ $host := promptOnceString "Enter the hostname for the right side:" -}}
{{ $direction := promptOnceString "Enter the direction:" -}}

release_bind = [ "KeyA", "KeyS", "KeyD", "KeyF" ]

port = 4242
frontend = "cli"

[{{ $direction }}]
hostname = {{ $host | quote }}
activate_on_startup = true
ips = []


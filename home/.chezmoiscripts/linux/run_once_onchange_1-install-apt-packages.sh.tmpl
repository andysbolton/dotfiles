#!/bin/bash

sudo apt-get update

{{ $packages := list
    "ca-certificates"
    "dnsutils" 
    "gdb"
    "gpg"
    "nmap"
    "unzip"
    "wget"
 
    # dependencies for installing python with asdf
    "zlib1g-dev"
    "openssl"
    "bzip2"
    "ncurses"
    "libffi"

    "make"
    "gcc"
    "whois" -}}

sudo apt-get install -y {{ $packages | join " " }}

